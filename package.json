{
  "name": "trustmebroai-local",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "local:setup": "node scripts/local-setup.mjs",
    "local:start": "node scripts/local-start.mjs",
    "local": "npm run local:setup && npm run local:start"
  }
}
